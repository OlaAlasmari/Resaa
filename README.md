
  # Create Low Fidelity Prototype

  This is a code bundle for Create Low Fidelity Prototype. The original project is available at https://www.figma.com/design/Tbv6qZZDBc3L366L0A8tLU/Create-Low-Fidelity-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  